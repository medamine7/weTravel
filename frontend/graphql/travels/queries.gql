query getTravels($limit: Int) {
  travels(limit: $limit) {
    title
    description
    images
    slug
  }
}

query getTravelBySlug($slug: String!) {
  travelBySlug(slug: $slug) {
    title
    description
    images
    slug
  }
}
